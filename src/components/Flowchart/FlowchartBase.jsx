// Have the flowchart 
// Functions: select, unselect, onmouse leave and enter
import flow-chart-renderer; 
const FlowchartBase = ((props) => { 
    const onSelect = (() => { 
        // Do the logic 
        // Update the context so other components can update 
    })
    const {data, onHover } = props; 
    return (<Flow /> )
})