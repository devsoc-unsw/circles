import data.json from 'path';
import hoverflor from "somewhere"; 


// 
const Flowchart = ((deg) => { 
    const deg = { deg }; // Getfrom context 
    const data = data blah;//
    const onHover = ((data ) => { 
        setHover=true
        return <hover data={data}></hover>
    })
    const onToggleShowAll = (() => { 

    })
   
    
    return (
        <FlowchartBase data={blah} onHover={onHover}/> 
        {hover == true && <InfoTopLeft data={data}/> }
    )

})