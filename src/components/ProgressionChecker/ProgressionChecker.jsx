import React from "react"; 
const ProgressionChecker = (() => 
    const state = // context get state 
    const onClick = (() => { 
        // Call function from context
    })
    return (
        <div> 
            Show all the selected nodes 
        </div>
    ) 
);