import React from "react"; 
const Test = (() => {
    const state = 0 // context get state 
    const onClick = (() => { 
        // Call function from context
    })
    return (
        <div> 
            Show all the selected nodes 
        </div>
    ) 
});