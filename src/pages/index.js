import * from react; 

const HomePage = (() => { 
    state = null 
    if (user choses a degree) set state => degree;
    return ( 
        <button> degree</button>
        <Flowchat /> 
        <DegreeProgression/> 
        <
    );
};