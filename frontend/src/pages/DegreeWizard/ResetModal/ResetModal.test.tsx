describe('ResetModal', () => {
  it('should render', () => {
    // const { getByText } = render(<ResetModal />);
    // expect(getByText('Reset Planner?')).toBeInTheDocument();
    fail('TODO');
  });

  it('should reset the planner', () => {
    fail('TODO');
  });

  it('should go back to course selector', () => {
    fail('TODO');
  });
});
