describe('SpecialisationStep', () => {
  it('should render', () => {
    fail('TODO');
  });

  it('should load up specialisation as options', () => {
    fail('TODO');
  });

  it('should call incrementStep after selecting years', () => {
    fail('TODO');
  });

  it('should show error notification when no program code is selected', () => {
    fail('TODO');
  });

  it('should show error notification when no specialisation is selected', () => {
    fail('TODO');
  });

  it('should navigate to course selector when successful', () => {
    fail('TODO');
  });
});
