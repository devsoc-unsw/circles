describe('StartBrowsingStep', () => {
  it('should render', () => {
    fail('TODO');
  });

  it('should show error notification when no program code is selected', () => {
    fail('TODO');
  });

  it('should show error notification when no specialisation is selected', () => {
    fail('TODO');
  });

  it('should navigate to course selector when successful', () => {
    fail('TODO');
  });
});
