describe('DegreeStep', () => {
  it('should render', () => {
    fail('TODO');
  });

  it('should call incrementStep after selecting degree', () => {
    fail('TODO');
  });

  it('should call fetchAllDegrees on mount', () => {
    fail('TODO');
  });

  it('should show no degree options initially', () => {
    fail('TODO');
  });

  it('should show options when user is searching degrees', () => {
    fail('TODO');
  });

  it('should update program code and name when selecting a degree option', () => {

  });
});
