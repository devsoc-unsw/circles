describe('CourseSelector', () => {
  it.todo('should render', () => {
    fail();
  });

  it.todo('should toggle locked courses', () => {
    fail();
  });

  it.todo('should show course description when click the course menu item', () => {
    fail();
  });

  it.todo('should be able to quick add a course from the course menu', () => {
    fail();
  });

  it.todo('should be able to quick remove a course from the course menu', () => {
    fail();
  });

  it.todo('should show rerender when user switch tabs', () => {
    fail();
  });

  it.todo(
    'should adds the course from the "Add To Planner" button in the course description',
    () => {
      fail();
    }
  );

  it.todo('should render course description based on the active tab', () => {
    fail();
  });

  it.todo('should open a course tab when the user searches for a course', () => {
    fail();
  });
});
