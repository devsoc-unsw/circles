describe('CourseSelector', () => {
  it('should render', () => {
    fail();
  });

  it('should toggle locked courses', () => {
    fail();
  });

  it('should show course description when click the course menu item', () => {
    fail();
  });

  it('should be able to quick add a course from the course menu', () => {
    fail();
  });

  it('should be able to quick remove a course from the course menu', () => {
    fail();
  });

  it('should show rerender when user switch tabs', () => {
    fail();
  });

  it('should adds the course from the "Add To Planner" button in the course description', () => {
    fail();
  });

  it('should render course description based on the active tab', () => {
    fail();
  });

  it('should open a course tab when the user searches for a course', () => {
    fail();
  });
});
