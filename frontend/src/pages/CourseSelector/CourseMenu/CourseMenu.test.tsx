describe('CourseMenu', () => {
  it('should render', () => {
    fail();
  });

  it('should show loading skeleton when still fetching course menu data', () => {
    fail();
  });

  it('should dispatch addTab when menu item is clicked', () => {
    fail();
  });
});
