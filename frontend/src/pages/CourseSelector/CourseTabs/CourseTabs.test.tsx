describe('CourseTabs', () => {
  it('should render', () => {
    fail();
  });

  it('should toggle locked courses switch', () => {
    fail();
  });

  it('should show tabs', () => {
    fail();
  });

  it('should dispatch setActiveTab', () => {
    fail();
  });

  it('should remove a tab when deleted', () => {
    fail();
  });

  it('should remove all tabs', () => {
    fail();
  });
});
