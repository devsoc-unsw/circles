import styled from "styled-components";

const Container = styled.div`
  padding: 2rem;
  max-width: 1500px;
  margin: auto;
`;

const TextBody = styled.div`
  font-size: 16px;
  padding-bottom: 10px;
`;

export default { Container, TextBody };
